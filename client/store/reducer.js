/* action constants
  const TEST = 'TEST';
*/

/* action creators
  export const testReducer = () => {
    return {
      type: TEST,
    };
  };
*/

// initial state
const initialState = {};

const reducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducer;
